# Auto-generated by sync-weekly-stable.sh — do not edit manually.
# Source: REPO_MANIFEST.toml in greenticai/.github
name: Weekly Stable Publish

on:
  pull_request:
    types: [closed]
    branches: [main]

permissions:
  contents: write

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

jobs:
  publish:
    if: github.event.pull_request.merged == true && contains(github.event.pull_request.labels.*.name, 'release')
    uses: greenticai/.github/.github/workflows/weekly-stable-publish.yml@main
    with:
      crates: "serde_yaml_gtc"
      merge-sha: ${{ github.event.pull_request.merge_commit_sha }}
    secrets: inherit
