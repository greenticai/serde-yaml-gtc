# Auto-generated by sync-weekly-stable.sh — do not edit manually.
# Source: REPO_MANIFEST.toml in greenticai/.github
name: Publish to crates.io

on:
  push:
    tags: ["v*"]
  workflow_dispatch: {}

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

jobs:
  publish:
    uses: greenticai/.github/.github/workflows/crates-publish.yml@main
    with:
      crates: "serde_yaml_gtc"
    secrets: inherit
