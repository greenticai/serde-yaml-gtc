# Auto-generated by .github/scripts/sync-deepseek-codex.sh — do not edit manually.
# Caller for the central greenticai/.github DeepSeek semver-remediation
# reusable (`codex-semver-fix.yml`). The file name keeps the `codex-` prefix
# until Phase G of plans/migrate-codex-deepseek.md so callers and the
# `notify-failure.yml` Slack-mute guard (`startsWith(workflow-name, 'Codex ')`)
# both stay valid through the silent pilot.
name: Codex Semver Fix

on:
  schedule:
    - cron: "33 3 * * *"
  workflow_dispatch:
    inputs:
      branch:
        description: "Branch to scan and patch (only for manual run)"
        required: false
        default: ""

permissions:
  contents: write
  pull-requests: write

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  semver-fix:
    if: github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository
    uses: greenticai/.github/.github/workflows/codex-semver-fix.yml@main
    with:
      branch: ${{ github.event.inputs.branch || '' }}
    secrets: inherit
